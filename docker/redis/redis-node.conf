# Cluster mode
cluster-enabled yes
cluster-config-file nodes.conf
cluster-node-timeout 5000
appendonly yes

# Network
bind 0.0.0.0
protected-mode no

# Performance
maxmemory 256mb
maxmemory-policy allkeys-lru